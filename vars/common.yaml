---
nfs_share: "base:/"
nfs_mount_opts: rw,noauto,noatime,hard,rsize=32768,wsize=32768

sites_home: /opt/sites
pves_home: /opt/pves
static_home: /opt/static

deploy_user: '{{ ansible_user_id }}'

logs_home: '{{ sites_home }}/logs'
